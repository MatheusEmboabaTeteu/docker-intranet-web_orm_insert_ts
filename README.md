# Docker-intranet-docker-intranet-web_orm_insert_ts.

# Autor: Matheus Emboaba Mello de Souza

## Installation

You need to create the docker container, to start the web application, so:

```console
docker-compose up
```